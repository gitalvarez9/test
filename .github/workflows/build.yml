name: sidou
on: workflow_dispatch
permissions:
  contents:write
jobs:
    corrector:
    tunson: ubuntu-latest
    steps:
      -uses: actions/checkout@v5
      -name: file generate
       run: date "+last build:%d/%m/%Y %H:%M UTC " >latsbuild.txt
      -name: Commit & push
       run:|
git config user.name "github_action{BOT}"
git config user.email "github-action{BOT}@user.noreply@github.com"
git add lastbuild.txt
git commit -m "autoupdate"||echo
git commit -m "nothing to commit"
git push
